# 🚀 Swap&Shop Webhook Server - Production Environment Configuration Example
# 
# WICHTIG: Diese Datei enthält sensible Produktionsdaten!
# - Niemals in Git committen
# - Sichere Backups erstellen
# - Regelmäßig rotieren

# Environment
ENVIRONMENT=production

# Server Configuration
PORT=8080
HOST=0.0.0.0
LOG_LEVEL=info
MAX_LOG_SIZE=100MB
MAX_LOG_FILES=10

# Stripe Configuration (LIVE KEYS)
STRIPE_PUBLISHABLE_KEY=pk_live_... # TODO: Echte Live Key eintragen
STRIPE_SECRET_KEY=sk_live_... # TODO: Echte Live Key eintragen
STRIPE_WEBHOOK_SECRET=whsec_live_... # TODO: Echte Live Webhook Secret eintragen

# Payrexx Configuration (LIVE KEYS)
PAYREXX_API_KEY=payrexx_live_... # TODO: Echte Live API Key eintragen
PAYREXX_WEBHOOK_SECRET=payrexx_live_... # TODO: Echte Live Webhook Secret eintragen

# Database Configuration (Supabase)
SUPABASE_URL=https://... # TODO: Echte Supabase URL eintragen
SUPABASE_ANON_KEY=... # TODO: Echte Supabase Anon Key eintragen
SUPABASE_SERVICE_ROLE_KEY=... # TODO: Echte Supabase Service Role Key eintragen

# Security Configuration
CORS_ORIGINS=https://swapshop.ch,https://app.swapshop.ch
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_MS=60000
MAX_REQUEST_SIZE=10MB

# Monitoring & Logging
ENABLE_METRICS=true
METRICS_PORT=9090
LOG_FORMAT=json
SENTRY_DSN=... # TODO: Sentry DSN eintragen (optional)

# Health Check Configuration
HEALTH_CHECK_INTERVAL=30s
HEALTH_CHECK_TIMEOUT=5s
HEALTH_CHECK_RETRIES=3

# Performance Configuration
MAX_CONCURRENT_REQUESTS=100
REQUEST_TIMEOUT_MS=30000
IDEMPOTENCY_CACHE_SIZE=10000
EVENT_LOG_MAX_SIZE=1000

# Backup & Recovery
BACKUP_ENABLED=true
BACKUP_INTERVAL=24h
BACKUP_RETENTION_DAYS=30
BACKUP_S3_BUCKET=... # TODO: S3 Bucket für Backups eintragen (optional)

# SSL/TLS Configuration
SSL_ENABLED=true
SSL_CERT_PATH=/etc/ssl/certs/swapshop.ch.crt
SSL_KEY_PATH=/etc/ssl/private/swapshop.ch.key
SSL_CA_PATH=/etc/ssl/certs/swapshop.ch.ca

# Firewall Configuration
ALLOWED_IPS=... # TODO: Erlaubte IP-Adressen eintragen (optional)
BLOCKED_IPS=... # TODO: Blockierte IP-Adressen eintragen (optional)

# Notification Configuration
SLACK_WEBHOOK_URL=... # TODO: Slack Webhook für Alerts eintragen (optional)
EMAIL_SMTP_HOST=... # TODO: SMTP Host für E-Mail-Alerts eintragen (optional)
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USER=... # TODO: SMTP Benutzer eintragen (optional)
EMAIL_SMTP_PASS=... # TODO: SMTP Passwort eintragen (optional)

# Maintenance Mode
MAINTENANCE_MODE=false
MAINTENANCE_MESSAGE="Swap&Shop Webhook-Server wird gewartet. Bitte versuchen Sie es später erneut."

# Feature Flags
ENABLE_STRIPE_WEBHOOKS=true
ENABLE_PAYREXX_WEBHOOKS=true
ENABLE_ANALYTICS=true
ENABLE_DEBUG_MODE=false
ENABLE_TEST_ENDPOINTS=false

# Custom Headers
CUSTOM_HEADERS=X-SwapShop-Version:1.0.0,X-SwapShop-Environment:production

# Timezone
TZ=Europe/Zurich

# Process Management
MAX_MEMORY_USAGE=512MB
MAX_CPU_USAGE=80%
RESTART_ON_FAILURE=true
RESTART_DELAY=5s 